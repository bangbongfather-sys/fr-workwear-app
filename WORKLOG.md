# NJ SAFETY 회사앱 — 작업 내역

> 최신 항목이 위. "작업내역 업데이트" 요청 시 이 파일에 이어서 기록.

## 2026-07-03

- **원가 레이더 (신규)** — 경영 대시보드에 마진 침식 조기 경보 카드 추가.
  최근 90일 실제 매입가(수량 가중평균, 최근 8건)와 단가표 기준가를 자동 비교해
  ±3% 이상 벌어진 자재·공임을 감지. 괴리 자재를 쓰는 제품별로 "납품가 유지 시
  실질 마진"(현재→실질)을 계산해 표시. [반영] 버튼으로 단가표를 실매입가로
  원클릭 업데이트, 제품 칩 클릭 시 단가 계산기로 이동. 괴리 없으면 자동 숨김.

## 2026-07-02

- **ERP 캡쳐 이미지 인식 입력** — 매입 현황 > 붙여넣기 패널에 이미지 입력 추가.
  ERP 화면 캡쳐를 붙여넣기(Ctrl+V)·드래그·업로드하면 Claude 비전이 표를 읽어
  품목명·수량·단가를 자동 추출 → 기존 미리보기·단가표 매칭 파이프라인으로 연결.
  (견적서 AI 생성에 쓰는 Claude API 키 공용)
- **모바일 반응형 재정비** — 실기기 크기(390px)로 전 탭 시각 감사 후 수정:
  - 모든 화면 왼쪽에 사이드바 잔상(검은 띠 12px)이 보이던 버그 수정
  - KPI 스탯 카드 1열 거대 스택 → 2열 컴팩트 그리드 (전 탭)
  - 서브탭 바 2~3줄 wrap → 한 줄 가로 스크롤 칩
  - 터치 기기에서 마우스 전용 UI(카드 리사이즈 핸들·안내 배너) 숨김
  - 탭 하이라이트 제거·좌우 고무줄 차단·PWA 노치 안전영역 등 폴리시

## 2026-06-28

- **자금 운용 거래처 검색 수정** — 통장 관리 거래 입력 폼의 거래처 칸이
  iPad/모바일 Safari에서 검색이 안 되던 문제(`<datalist>` 미지원) → 커스텀
  검색 콤보박스로 교체. 거래처+공급처+기존 거래내역 통합 후보.
- **오프라인 저장 유실 수정** — 비행기모드에서 작업 후 앱을 껐다 켜면 재연결돼도
  동기화가 안 되던 버그. 미동기화 섹션 목록을 localStorage에 보존해
  오프라인 재시작에도 복원 → 재연결 시 자동 푸시.

## 2026-06-27

- **모바일 흰화면 긴급 수정** — 구형 모바일 Safari에서 SyntaxError로 앱이 안 뜨던
  문제. 원인: Babel 버전 미지정 → 신규 배포된 Babel 8이 구형 Safari 비호환 코드
  생성. `@babel/standalone@7`로 고정 + 서비스워커 캐시 v3 갱신.
- **매입 등록 개선** — ① 매입일 날짜칸이 iOS에서 비정상적으로 크게 나오던 것 수정
  ② 품목 선택에 검색 콤보박스(단가표 자재·공임 필터, 선택 시 단가·단위 자동 채움)
  ③ 공급업체 칸에도 거래처 검색 추가.
- **동기화 충돌 처리 개선 (섹션별 자동 병합)** — 데이터를 24개 섹션 단위로 나눠
  버전 관리. 서로 다른 메뉴를 두 기기에서 동시 수정하면 자동 병합(충돌 없음),
  같은 항목을 동시 수정할 때만 충돌 표시. 바뀐 섹션만 부분 저장.
- **동기화 속도 개선** — 저장 대기 1.5초 → 0.6초. 8초 주기 경량 폴링 추가로
  다른 기기 변경이 화면 안 나가도 자동 반영.

## 2026-06-23

- **디자인 핸드오프 적용** — 오렌지 토큰 `#f97316` 계열로 전면 교체, 다크모드
  팔레트 갱신, 숫자·금액에서 디스플레이 폰트 제거(산세리프+tabular 규칙),
  단가 계산기 "왼쪽 입력 / 오른쪽 통합 결과" 구조 개편(38px 단가 히어로,
  원가 구성 스택 바, ①②③ 섹션 배지).
- **인상 시뮬레이터** — 영향 요약 등급 필터에서 "전체" 제거, A/B/C/D만 유지.

## 2026-06-22 이전

- **인상 시뮬레이터** — 마지막 작업 상태 자동 저장, 시나리오 프로젝트
  저장·비교·불러오기, 초기화 버튼(저장 프로젝트는 유지).
- **부분 입금 날짜 선택 버그 수정** — iOS Safari에서 날짜 피커가 안 뜨던 문제.
- **연매출 추이 누적 비교(YTD) 뷰** — 연도별 1~N월 누적·단독 비교 테이블/차트.
- **오프라인(PWA) 지원** — 서비스워커 캐시 전략 + frw_dirty 보호로 비행기모드 작업 지원.
